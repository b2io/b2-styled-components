import { BorderRadius } from './Base';
import Color from './Color';

const Theme = {
  borderRadius: `${BorderRadius.default}`,
  bg: `${Color.accent}`,
  fg: `${Color.white}`,
};

export default Theme;
